<ContentDialog
    x:Class="LocaTracker2.Views.Dialogs.CreateTripDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LocaTracker2.Views.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Create Trip"
    PrimaryButtonText="Create Trip"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">
    <Grid>
        <StackPanel>
            <TextBlock Text="Enter a name and an optional description for your new trip:" 
                       TextWrapping="Wrap"
                       Margin="0,10"
                       />
            <TextBox x:Name="TripNameTextBox" 
                     PlaceholderText="Trip Name (required)"
                     />
            <TextBox x:Name="TripDescriptionTextBox" 
                     PlaceholderText="Trip Description (optional)" 
                     TextWrapping="Wrap"
                     Height="150"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     />
        </StackPanel>
    </Grid>
</ContentDialog>
